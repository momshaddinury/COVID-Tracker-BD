import 'package:flutter/material.dart';

class Survery extends StatefulWidget {
  @override
  _SurveryState createState() => _SurveryState();
}

class _SurveryState extends State<Survery> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'Survey Tab is under development'
        ),
      ),
    );
  }
}
